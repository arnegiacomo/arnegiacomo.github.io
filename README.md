# arnegiacomo.dev

Personal site. Plain neobrutalistic HTML, CSS and a little JavaScript on GH Pages.

Run locally with `python3 dev.py` for dev.

Regenerate the share card and profile banner with `python3 gen-images.py --banner`.
